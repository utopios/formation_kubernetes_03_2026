apiVersion: v1
kind: Service
metadata:
  name: api-nodeport
  namespace: dev
spec:
  type: NodePort
  selector:
    app: api-backend
  ports:
    - port: 80
      targetPort: 5678
      nodePort: 30080
